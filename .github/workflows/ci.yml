name: CI

on:
  push:
    branches:
      - master

jobs: 
    build:
      name: 'Build and Push'
      runs-on: ubuntu-latest
      strategy:
        matrix:
          node: [22]

      steps:
      - name: Checkout code | ${{matrix.node}}
        uses: actions/checkout@v6

      - name: Setup node
        uses: actions/setup-node@v6
        with:
          node-version: ${{matrix.node}}
          cache: 'yarn'
      
      - run: yarn
      - run: yarn run test

      - name: Generate tag
        id: generate_tag
        run: |
          SHA=$(echo $GITHUB_SHA | head -c7)
          echo "sha=$SHA" >> $GITHUB_OUTPUT

          

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v6.1.0
        with:
          aws-region: us-east-2
          role-to-assume: ${{ secrets.AWS_ROLE }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      #- name: Build, tag, and push docker image to Amazon ECR
      #  env:
      #    REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      #    REPOSITORY: my-ecr-repo
      #    IMAGE_TAG: ${{ steps.generate_tag.outputs.sha }}
      #  run: |
      #    docker build -t $REGISTRY/$REPOSITORY:$IMAGE_TAG .
      #    docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG



      #- name: Login to Docker Hub
      #  uses: docker/login-action@v4
      #  with:
      #    username: ${{ secrets.DOCKERHUB_USERNAME }}
      #    password: ${{ secrets.DOCKERHUB_TOKEN }}

      #-
      #  name: Build and push
      #  uses: docker/build-push-action@v7
      #  with:
      #    push: true
      #    tags: jonasportes/rocket-ci-api:${{steps.generate_tag.outputs.sha}},jonasportes/rocket-ci-api:latest


      # - name: Build docker image
      #   run: docker build -t jonasportes/rocket-ci-api:${{steps.generate_tag.outputs.sha}} .

      # - name: Push Image
      #   run: docker push jonasportes/rocket-ci-api:${{steps.generate_tag.outputs.sha}}


